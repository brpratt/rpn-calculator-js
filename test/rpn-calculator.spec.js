const {expect} = require('chai');
const RpnCalculator = require('../src/rpn-calculator');

describe('RpnCalculator', () => {
  it('Should do something...', () => {
    expect('test driven development').to.include('drive');
  });  
});
